name: 02 - Workflow Events

on:
    workflow_dispatch:

jobs:
    echo:
        runs-on: ubuntu-latest
        steps:
            - name: triggered by event
              run: echo "show who triggered this ${{ github.event_name }} event."
